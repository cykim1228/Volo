# Volo 런타임 의존성 (핵심 엔진 + CLI).
# 권위 있는 정의는 pyproject.toml. 이 파일은 빠른 개발 설치 편의용이다.
#   pip install -r requirements.txt
#
# 참고: 무거운 의존성 없이도 결정적 모듈(models/segment/export)과 테스트는 import 가능하다
# (volo_engine/__init__.py 의 얕은 import 정책). 아래는 실제 파이프라인 실행에 필요.

# --- STT (로컬 추론, CTranslate2) ---
faster-whisper>=1.0.0

# --- ffmpeg 바이너리 폴백 (시스템 PATH 없을 때 번들 바이너리 제공) ---
imageio-ffmpeg>=0.5.1

# --- 데스크톱 UI (Phase 2; pip install -e .[app] 로도 설치 가능) ---
PySide6>=6.6.0

# --- 개발/테스트 ---
pytest>=8.0
ruff>=0.5.0
